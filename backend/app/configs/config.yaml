inference:
  use_triton: false

triton:
  url: localhost:8000
  model_version: ""
  request_timeout: 10.0

models:
  weights_dir: weights

captures:
  enabled: true
  dir: captures/screenshots
  dir: captures

qdrant:
  url: http://localhost:6333
  api_key: ""
  collection: face_embeddings
  vector_size: 512
  top_k: 5
  max_registration_images: 5
